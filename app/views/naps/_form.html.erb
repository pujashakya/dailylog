<%= form_for(nap) do |f| %>
  <% if nap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nap.errors.count, "error") %> prohibited this nap from being saved:</h2>

      <ul>
      <% nap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nap_date %>
    <%= f.datetime_select :nap_date, class:"form-control input-lg", required:true%>
  </div>

  <div class="field">
    <%= f.label :duration %>
    <%= f.text_field :duration,class:"form-control input-lg", required:true %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_field :notes ,class:"form-control input-lg"%>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
